This directory contains files which are loaded before the rest of the applicaton.

For example, code which sets the user's theme, so as to prevent the document's background
from flashing white in-between the initial load of the page and bootstrap of the app.
