  // const sorted = foundKanji.sort((a, b) => {
  //   if (Array.isArray(b.stroke)) {
  //     return +a.stroke >= +Math.min(...b.stroke.map(x => +x)) ? -1 : 1
  //   }

  //   return +a.stroke >= +b.stroke ? -1 : 1
  // })
